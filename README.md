# Portfolio Connect

A minimal static portfolio website with a frosted glass design.

Built for simple public access via GitHub Pages, it provides a clean way to present projects, links, and documentation in one place.

## Structure

* `index.html` – main page
* `styles.css` – styling and layout
* `assets/docs/` – PDF documentation
